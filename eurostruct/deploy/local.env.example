# EUROSTRUCT — gabarit de l'exécution locale de production.
#
#     cp deploy/local.env.example deploy/local.env
#     # remplir, puis:
#     docker compose --env-file deploy/local.env up --build
#
# CE FICHIER EST UN GABARIT, ET IL EST VERSIONNE A CE TITRE. Il ne porte
# AUCUNE valeur réelle: ni mot de passe, ni clé, ni adresse d'instance. La
# copie remplie, elle, est ignorée par Git — voir `.gitignore`.
#
# UN CHAMP VIDE N'EST PAS UNE VALEUR PAR DEFAUT. L'API refuse de se déclarer
# prête sans configuration d'authentification, et `/ready` dit précisément ce
# qui manque, sans jamais recopier une valeur.

# --- base de données (locale, jetable) --------------------------------------
# Ce mot de passe ne protège qu'un conteneur sans port publié. Il n'en reste
# pas moins un mot de passe: on en met un, et on ne le commite pas.
#
# `POSTGRES_USER` EST LE SUPERUTILISATEUR DE L'IMAGE, et il ne sert QU'A
# l'initialisation — créer des rôles est le seul acte qui l'exige. L'API ne le
# voit jamais: RLS ne s'applique pas à un superutilisateur, et toutes les
# politiques que les migrations posent seraient décoratives.
POSTGRES_USER=eurostruct_super
POSTGRES_PASSWORD=
POSTGRES_DB=eurostruct

# --- les trois logins que l'initialisation crée ------------------------------
# Trois rôles, trois pouvoirs distincts, et c'est la séparation que le
# déploiement en trois phases suppose:
#
#   * le PLAN DE CONTROLE pose le sceau et finalise; il n'applique aucune
#     migration;
#   * le MIGRATEUR applique les migrations, et n'a aucune capacité résiduelle
#     après la finalisation;
#   * le LOGIN APPLICATIF est celui que l'API présente à chaque requête. Il
#     n'a ni `createrole` ni `createdb`, et n'atteint que les primitives.
EUROSTRUCT_PLAN_DB_USER=eurostruct_plan
EUROSTRUCT_PLAN_DB_PASSWORD=
EUROSTRUCT_MIGRATOR_DB_USER=eurostruct_migrator
EUROSTRUCT_MIGRATOR_DB_PASSWORD=
EUROSTRUCT_APP_DB_USER=eurostruct_app
EUROSTRUCT_APP_DB_PASSWORD=

# --- ce qui n'existe QUE pour une composition locale --------------------------
# En production, `auth.users` et `auth.uid()` viennent de Supabase. Ce drapeau
# demande un schéma FICTIF à leur place, pour un poste ou un runner où aucun
# Supabase ne tourne. Le laisser à `non` en production n'est pas une précaution:
# c'est la seule valeur correcte.
EUROSTRUCT_LOCAL_AUTH_STUB=non

# --- le mandat d'amorçage de la racine d'autorité -----------------------------
# Forme « <uuid du principal>:<empreinte du document de mandat> ». SANS LUI,
# AUCUNE RACINE N'EST AMORCEE, et c'est le comportement correct: désigner la
# première personne habilitée est une décision prise hors du système. Les
# primitives existent, mais aucune habilitation n'est efficace et toute
# proposition est refusée.
EUROSTRUCT_BOOTSTRAP_MANDATE=
EUROSTRUCT_BOOTSTRAP_ACTOR=
EUROSTRUCT_BOOTSTRAP_NAME=
EUROSTRUCT_BOOTSTRAP_REASON=

# --- vérification des jetons (côté API, secrets de RUNTIME) ------------------
# L'URL du JWKS de l'émetteur, son `iss` et son `aud`. Sans les trois,
# `/ready` reste rouge et les routes d'autorité rendent 503 — c'est voulu.
EUROSTRUCT_SUPABASE_JWKS_URL=
EUROSTRUCT_SUPABASE_ISSUER=
EUROSTRUCT_SUPABASE_AUDIENCE=authenticated
# ALGORITHMES ASYMETRIQUES UNIQUEMENT. La configuration refuse HS256: avec un
# secret partagé, quiconque peut vérifier peut aussi signer.
EUROSTRUCT_JWT_ALGORITHMS=RS256

# --- ce que le NAVIGATEUR doit joindre (public par nature) -------------------
# Ce ne sont pas les noms du réseau interne: c'est le navigateur de
# l'utilisateur qui appelle, et il ne connaît ni `api` ni `db`.
#
# Aucune de ces trois valeurs n'est inlinée dans l'image: le layout les lit à
# chaque requête et les dépose dans la page. Les changer est un redémarrage,
# pas une reconstruction.
EUROSTRUCT_PUBLIC_API_URL=http://127.0.0.1:8000
EUROSTRUCT_PUBLIC_SUPABASE_URL=
# La clé ANONYME de GoTrue. Elle désigne un projet et n'autorise rien par
# elle-même — ne jamais mettre ici une clé de service.
EUROSTRUCT_PUBLIC_SUPABASE_ANON_KEY=

# --- origines et ports ------------------------------------------------------
# Déclarées une à une. `*` est REFUSE par la configuration: un joker laisserait
# n'importe quelle page du web appeler l'API depuis le navigateur d'un
# utilisateur connecté.
EUROSTRUCT_CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
API_PORT=8000
WEB_PORT=3000

# --- le magasin d'objets (livrables) ----------------------------------------
# LES LIVRABLES NE VIVENT PLUS SUR UN DISQUE DE CONTENEUR. Deux instances d'API
# derrière un répartiteur ne partagent pas un disque : la moitié des
# téléchargements rendrait 503 sur des livrables parfaitement enregistrés.
#
# `local` reste possible pour un poste sans magasin objet. `s3` est la valeur
# de la composition, et elle NE RETOMBE PAS sur le disque : une configuration
# incomplète fait refuser la création d'un livrable par un 503 explicite plutôt
# qu'écrire des octets là où ils disparaîtront.
EUROSTRUCT_STORAGE_BACKEND=s3

# Le compartiment est PRIVÉ, et il est provisionné par la composition — jamais
# par le produit. Un service qui créerait le sien au démarrage masquerait une
# erreur de configuration : il écrirait dans un compartiment neuf au lieu de
# refuser parce que celui qu'on a nommé n'existe pas.
EUROSTRUCT_S3_ENDPOINT=http://objets:9000
EUROSTRUCT_S3_REGION=us-east-1
EUROSTRUCT_S3_BUCKET=eurostruct-livrables
EUROSTRUCT_S3_PREFIX=livrables
# Style CHEMIN (`http://hôte/compartiment/clé`). Le style « hôte virtuel »
# exigerait une entrée DNS par compartiment, ce qu'un MinIO local n'a pas.
EUROSTRUCT_S3_PATH_STYLE=oui

# CES DEUX VALEURS SONT DES SECRETS, ET CE GABARIT NE LES PORTE PAS. La
# composition les donne AUSSI à MinIO : le magasin et son client doivent
# présenter les mêmes, sinon la pile démarre parfaitement et refuse chaque
# dépôt.
EUROSTRUCT_S3_ACCESS_KEY_ID=
EUROSTRUCT_S3_SECRET_ACCESS_KEY=

# TLS. En clair sur le réseau interne d'une composition locale ; chez un
# fournisseur, l'endpoint est en `https://` et la vérification reste à `oui`.
# La désactiver est une décision qui doit s'écrire.
EUROSTRUCT_S3_VERIFY_TLS=oui
EUROSTRUCT_S3_CA_BUNDLE=

# Chiffrement au repos, côté serveur. Vide en local : MinIO sans KMS refuserait
# chaque dépôt si l'on déclarait `aws:kms` sans clé. Chez un fournisseur qui le
# porte : `AES256`, ou `aws:kms` avec l'identifiant de clé.
EUROSTRUCT_S3_SSE=
EUROSTRUCT_S3_SSE_KMS_KEY_ID=
